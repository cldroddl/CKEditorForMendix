# 코드 리뷰 — `react-ver` 브랜치

- 날짜: 2026-09-03
- 대상: `master...react-ver` 전체 diff (`pluggable/` 아래 React + CKEditor 5 재작성, `package-lock.json` 제외 ~2100줄)
- 방식: 신규 소스 전 파일 정독 + 에디터/뷰어/shared 데이터 흐름 추적
- 참고: 자동 verify 패스 없이 1차 리뷰. #2·#7·#9는 실기동(Studio Pro) 확인 권장.

## 요약

| 심각도 | 건수 | 항목 |
| --- | --- | --- |
| 🔴 High | 3 | #1 Viewer XSS, #2 microflow-link 드롭다운 미동작, #3 프로그래밍적 값 갱신이 마이크로플로우 오발동 |
| 🟡 Medium | 4 | #4 이미지 재다운로드, #5 enterMode 무효, #6 codeBlock 툴바 아이템 없음, #7 레거시 `__LINK__` upcast 충돌 |
| 🟢 Low | 3 | #8 keyTimer 미정리, #9 spellChecker 미반영, #10 글자 수 UI 빈 div |

대부분 "아직 미완성"(`pluggable/MIGRATION.md`의 Phase 2~3)이지만 **#1·#2·#3**은 스캐폴드 단계에서도 고쳐야 하는 실제
버그입니다. 특히 **#1(XSS)은 배포 전 필수**입니다.

---

## 🔴 High

### #1 — Viewer XSS: 무필터 `dangerouslySetInnerHTML` + 와일드카드 GHS

- 파일: `pluggable/packages/shared/src/RichTextView.tsx:85`, `pluggable/packages/rich-text/src/components/Editor.tsx:121`
- 내용: 뷰어가 저장된 HTML을 sanitization 없이 `dangerouslySetInnerHTML`로 렌더합니다. 동시에 에디터의
  `htmlSupport.allow`가 `[{ name: /.*/, attributes: true, classes: true, styles: true }]` — 완전 와일드카드라
  위험한 속성이 그대로 저장됩니다.
- 시나리오: 에디터 사용자(또는 붙여넣기/임포트/마이크로플로우로 세팅된 콘텐츠)가
  `<img src=x onerror=alert(document.cookie)>` 또는 `<a onclick=...>` 를 바인딩된 attribute에 저장. `RichTextViewer`
  마운트 시 해당 HTML을 그대로 주입 → 스크립트 실행. CKEditor 4는 ACF로 필터링했으나 새 스택엔 없음. Mendix 공식
  rich-text 위젯은 렌더 시 sanitize함.
- 권고: 뷰어에 DOMPurify 등 sanitizer 도입, 그리고/또는 GHS `allow`를 필요한 태그·속성 화이트리스트로 축소
  (`data-mf`, `class`, `href`, `title`, `img[src,alt,width,height,data-image-guid]` 등).

### #2 — microflow-link 툴바 드롭다운이 링크를 삽입하지 못함

- 파일: `pluggable/packages/rich-text/src/plugins/MendixLink/MendixLinkUI.ts:54`
- 내용: 드롭다운 `execute` 핸들러가 `(evt.source).mendixLinkName`을 읽지만, 이 커스텀 속성은 `ViewModel`에만
  설정되고 `addListToDropdown`이 생성하는 `ButtonView`(표준 prop만 바인딩)에는 전달되지 않습니다.
- 시나리오: 앱이 microflow 링크를 구성하고 에디터 사용자가 "Microflow link" 툴바 드롭다운에서 항목을 클릭 →
  `evt.source`는 생성된 `ListItemButtonView` → `evt.source.mendixLinkName === undefined` → `if (name)` 가드 실패 →
  `editor.execute('mendixLink', ...)` 미호출 → 링크가 삽입되지 않음 (이 위젯의 핵심 기능).
- 권고: 링크 이름을 `ViewModel`에 담아 `items` 컬렉션의 인덱스/모델과 매핑하거나, `dropdown.on('execute')` 대신 각
  버튼 모델에 개별 핸들러를 바인딩. 실기동 확인 필요.

### #3 — 프로그래밍적 값 갱신이 onChange/onKeyPress 마이크로플로우를 발동시키고 attribute를 dirty로 만듦

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:160`
- 내용: `onChange` 핸들러가 `props.onKey(html)`를 무조건 호출하고, 정규화된 HTML을 attribute에 다시 커밋합니다.
- 시나리오: 마이크로플로우가 `content.setValue(...)`를 호출하거나 컨텍스트 객체가 새로고침됨 → CKEditor react 래퍼가
  `editor.setData(newValue)` 호출 → `change:data` emit → `Editor.onChange` 실행: `props.onKey(html)`가
  `onKeyPress.execute()` 예약; `getData()`는 CKEditor 정규화 HTML을 반환해 저장값과 달라짐 → `html !== content.value`
  → `commit()`이 `content.setValue(normalized)` 호출 → attribute dirty, 다음 렌더에서 1회 추가 쓰기 발생. 무한 루프는
  아니지만 사용자 입력 없이 마이크로플로우가 실행되고 폼이 dirty 상태가 됨.
- 권고: "사용자에 의한 변경"만 커밋/발동하도록 구분 (CKEditor의 change source 확인, 또는 `setData` 직전에 억제
  플래그 설정). `onKey`는 실제 key 이벤트에만 연결.

---

## 🟡 Medium

### #4 — 렌더마다 모든 이미지 재다운로드 + 깜빡임

- 파일: `pluggable/packages/shared/src/imageUrls.ts:23`
- 내용: `getFileUrl()`이 매 호출 `Date.now()`를 캐시버스터로 사용하고, `resolveImageUrls()`가 매 렌더 실행됩니다
  (`wireUp`/`links` memo identity가 계속 바뀜).
- 시나리오: `RichTextViewer` 재렌더 (Mendix가 매 렌더 `microflowLinks` 배열 재생성 → `links` useMemo 재계산 →
  `wireUp` useCallback 변경 → useEffect 재실행). `resolveImageUrls`가 매번 `img.src`를
  `file?guid=123&changedDate=<새 Date.now()>` 로 재설정 → 브라우저가 매 렌더마다 모든 이미지 재다운로드 → 트래픽 폭증
  + 시각적 깜빡임.
- 권고: 캐시버스터를 안정적인 값(문서 changedDate 등)으로, 그리고 이미 해석된 이미지는 재처리 스킵
  (`img.dataset.resolved` 마킹) 또는 `useMemo`로 `links` 안정화.

### #5 — `enterMode`가 사실상 무효

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:105`
- 내용: `enterMode==='BR'`은 ShiftEnter 플러그인만 추가(이건 Enter가 아니라 Shift+Enter를 담당), `enterMode==='P'`는
  ShiftEnter를 아예 빼서 soft line break를 못 쓰게 만듦.
- 시나리오: Dojo 위젯에서 마이그레이션하는 앱이 Enter mode = BR로 설정, Enter가 `<br>`를 만들 것으로 기대. 여전히
  새 `<p>`가 생성됨. 별개로 기본 P 모드에서는 ShiftEnter가 로드되지 않아 Shift+Enter가 soft break를 삽입하지 못함
  (레거시 대비 회귀).
- 권고: CKEditor 5에는 `enterMode` 직접 대응이 없음을 문서화하고, ShiftEnter는 항상 로드. "Enter = BR"은 지원 불가
  또는 커스텀 처리 필요.

### #6 — `codeBlock` 활성화해도 툴바 버튼이 없음

- 파일: `pluggable/packages/shared/src/toolbarPresets.ts:9`
- 내용: basic/standard/full 프리셋 어디에도 `codeBlock` 툴바 아이템이 없습니다.
- 시나리오: 앱 개발자가 "Code blocks"(codeBlock=true) 활성화 → CodeBlock이 plugins에 추가되지만 `resolveToolbar()`가
  non-custom 프리셋에 `codeBlock`을 넣지 않아 버튼이 안 보임. Custom 툴바로 바꾸고 정확한 아이템명을 알아야만 사용
  가능.
- 권고: `codeBlock` prop이 true이면 standard/full 프리셋에 `codeBlock` 아이템을 조건부로 삽입.

### #7 — 레거시 `href="__LINK__"`가 Link 플러그인에 의해 upcast되어 재저장 시 placeholder가 잔존

- 파일: `pluggable/packages/rich-text/src/plugins/MendixLink/MendixLinkEditing.ts:23`
- 내용: 레거시 microflow 앵커는 `href="__LINK__"`를 가짐. core Link 플러그인이 이를 `linkHref`로 upcast하고 다시
  downcast하므로, 재저장된 콘텐츠에 `__LINK__` placeholder가 href에 남고 MendixLink downcast와 충돌해 중첩/평문
  링크로 렌더될 수 있음.
- 시나리오: 기존 저장 콘텐츠
  `<a href="__LINK__" class="mx-microflow-link" onclick="CKEditorViewer.mf.exec('Fn',...)">` 를 새 에디터에서 열기 →
  Link가 href를 `linkHref="__LINK__"`로 upcast → 저장 시 에디터가 여전히 `href="__LINK__"`(그리고 중첩 링크 요소
  가능)를 포함한 `<a>`를 emit → 뷰어의 `migrateStoredValue`가 더 이상 매칭 안 됨(`onclick` 문자열이 사라짐) →
  잔여 placeholder href가 남음.
- 권고: 레거시 앵커 upcast 시 `href`를 `#`로 정규화(MendixLinkEditing의 legacy upcast에서 처리), 또는 Link 플러그인
  보다 높은 우선순위로 `mx-microflow-link` 앵커를 가로채기.

---

## 🟢 Low

### #8 — `keyTimer` 디바운스 타이머가 언마운트 시 정리되지 않음

- 파일: `pluggable/packages/rich-text/src/RichText.tsx:32`
- 내용: cleanup useEffect가 없어 `keyTimer`가 언마운트 시 `clearTimeout`되지 않음.
- 시나리오: 사용자가 에디터에 타이핑 후 250ms 내에 위젯이 닫힘/페이지 이탈 → 대기 중이던 `setTimeout`이 언마운트 후
  발동해 `onKeyPress.execute()`를 잘못된 시점에 호출.
- 권고: `useEffect(() => () => clearTimeout(keyTimer.current), [])` 추가.

### #9 — `spellChecker` prop이 CKEditor config에 반영되지 않음

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:53`
- 내용: `spellChecker`가 `EditorProps`로 전달되지만 config나 editing view에 적용되지 않음.
- 시나리오: 앱 개발자가 "Browser spell checker"를 false로 설정 (레거시 `enableSpellCheck` 대응 기대)하지만 아무것도
  `props.spellChecker`를 읽지 않아 editable이 `spellcheck="true"`를 유지.
- 권고: editing view root에 `spellcheck` 속성을 설정하거나 관련 config 적용.

### #10 — 글자 수 UI가 빈 div만 렌더

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:170`
- 내용: 글자 수 UI가 빈 `<div className="rt-count">`를 렌더하고, `wordCount.onUpdate`는 `rt-count--over` 클래스만
  토글함. 실제 글자 수 값이 요소에 기록되지 않음.
- 시나리오: 앱 개발자가 "Show character count" 활성화 → 에디터 아래 빈 div가 나타남 → maxCount 초과 시 빨간색이
  되지만 숫자 자체는 표시 안 됨 → 사용자는 숫자 없는 빨간 막대만 봄.
- 권고: `onUpdate`에서 `el.textContent`에 `characters` 값을 기록, 또는 CKEditor `WordCount` 플러그인의
  `wordCountContainer` 요소를 마운트.
