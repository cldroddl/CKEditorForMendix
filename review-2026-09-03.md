# 코드 리뷰 — `ckeditor4-react` 브랜치

- 날짜: 2026-09-03
- 대상: `react-ver...ckeditor4-react` diff (에디터 위젯을 CKEditor 5 → **CKEditor 4.22.0** 으로 교체한 델타, ~700줄)
- 방식: 신규 CKEditor 4 소스 정독 + `node_modules/ckeditor4/ckeditor.js` 직접 대조
- 참고: `react-ver` 브랜치 리뷰는 별도 (같은 날짜, 해당 브랜치의 `review-2026-09-03.md`). `packages/shared` 와
  `packages/rich-text-viewer` 는 `react-ver` 와 동일하므로 그쪽 리뷰의 shared/viewer 항목이 이 브랜치에도 적용됨 —
  아래 "상속되는 이슈" 참고.

## ⚠️ 사실 오류: `standard` vs `standard-all`

`ckeditor4@4.22.0` npm 패키지는 **`standard` 프리셋**입니다 (`standard-all` 아님). 문서(`RichText.xml`,
`MIGRATION.md`, `README.md`)와 기본 CDN URL에 "standard-all"이라 쓴 것은 틀림. `standard` 빌드에는 autogrow, font,
colorbutton, justify, find 등이 번들되어 있지 않음. → C3·C7 과 연결.

## 요약

| 심각도 | 건수 | 항목 |
| --- | --- | --- |
| 🔴 High | 4 | C1 ACF 콘텐츠 스트립, C2 폼 열 때 마이크로플로우 오발동, C3 번들에 없는 플러그인 의존, C6 선택 텍스트 삭제 |
| 🟡 Medium | 4 | C4 rejected promise 영구 캐시, C5 정규화 차이로 setData 반복, C7 문서/패키지 불일치, C10 effect deps 누락 |
| 🟢 Low | 2 | C8 `replace()` null 미가드, C9 maxCount 죽은 코드 |

---

## 🔴 High

### C1 — CK4 ACF가 기존 저장 HTML을 조용히 스트립

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:70`
- 내용: CK5 config는 `htmlSupport.allow = [{ name: /.*/, attributes: true, classes: true, styles: true }]` 로 모든
  것을 허용했음. CK4 포팅은 `extraAllowedContent: "a[data-mf,href,title,class];*[data-*]"` 만 설정하고
  `allowedContent: true` 를 켜지 않음. CK4 자동 ACF는 기본 ON → 로드/저장 시마다 인라인 스타일, 비허용 클래스,
  `<div>`/`<span>` 래퍼, 이미지/테이블 속성이 제거됨.
- 추가 문제: `class` 를 `[...]`(속성 브래킷)에 넣었으나 CK4 클래스 문법은 `a(*)` — 따라서 microflow-link 버튼
  클래스(`btn btn-default mx-button`)조차 유지되지 않을 수 있음.
- 시나리오: 기존 Mendix 콘텐츠(인라인 스타일, 커스텀 클래스, div 래퍼 포함)를 새 에디터에서 열면 첫 로드와 이후 모든
  저장에서 해당 마크업이 사라짐.
- 권고: CK5 와의 패리티를 원하면 `allowedContent: true` 로 ACF 비활성화(단 C1은 XSS 우려와 상충 — 뷰어 sanitize와
  함께 검토). 최소한 클래스는 `a(*)`, 스타일은 필요 태그에 `{*}` 로 명시.

### C2 — 폼을 열 때마다 `onKeyPress` (그리고 종종 `onChange`) 마이크로플로우 발동

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:82`
- 내용: `instanceReady` 에서 `instance.setData(value)` 호출. `ckeditor.js` 확인 결과 CK4 는 `setData → dataReady →
  saveSnapshot → UndoManager.save()` 경로로 (빈 내용 → 실내용 변화 감지) **`change` 이벤트를 발생**시킴.
- 시나리오: 부착된 `emit()` 이 마운트 시 실행 → 항상 `propsRef.current.onKey(html)` → `RichText.onKey` → `fireKey()`
  → 250ms 후 `onKeyPress.execute()` → 폼 열 때마다 "On key press" 마이크로플로우 실행. 정규화된 `getData()` 가 raw
  저장값과 다르면 `onChange(html)` → `content.setValue()` 도 호출되어 편집 없이 Mendix attribute 가 dirty 상태가 됨.
  `lastEmitted.current` 는 raw `p.value` 로 설정되고 정규화 값으로 재조정되지 않음.
- 권고: `setData` 직전에 억제 플래그를 세워 그 사이의 `change` 를 무시, 그리고 `lastEmitted` 를 `instanceReady`
  이후 `instance.getData()` 로 초기화. `onKey` 는 실제 사용자 편집에만 연결.

### C3 — `autoGrow_*` / "full" 프리셋 버튼이 번들에 없는 플러그인에 의존

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:60`
- 내용: `node_modules/ckeditor4` 4.22.0 `ckeditor.js` 는 `standard` 프리셋 (autogrow/font/colorbutton/justify/find
  미번들 — 확인함). `extraPlugins` 에는 `mendixlink` 만 있음.
- 시나리오: `autoGrow_onStartup`/`autoGrow_minHeight`/`autoGrow_maxHeight` 를 설정해도 autogrow 플러그인이 없어
  아무 효과 없음 → 에디터가 ~200px 근처에 고정되고 `minHeight` 속성이 무시됨. `maxHeight=0`(기본)이면 `config.height`
  도 설정 안 됨. 문서화된 자체 호스팅 배포(`node_modules/ckeditor4/` 호스팅)에서도 "full" 툴바가 Font, FontSize,
  TextColor, BGColor, JustifyLeft/Center/Right/Block, Find, Replace, Iframe, ShowBlocks 를 조용히 잃음.
- 권고: 필요한 플러그인을 `CKEDITOR.plugins.addExternal` 로 추가하거나 "full" 빌드(예: `standard-all` / `full-all`)
  로 자체 호스팅을 전제. 프리셋을 실제 번들에 맞게 축소하거나, 누락 시 경고.

### C6 — 선택 텍스트 위에 microflow 링크 삽입 시 선택 텍스트가 삭제됨

- 파일: `pluggable/packages/rich-text/src/ckeditor4/mendixLinkPlugin.ts` (`onShow` / `onOk`)
- 내용: `onShow` insert 모드는 빈 `<a>` 를 만들고 `setupContent` 를 건너뜀 → "Link text" 필드가 선택과 무관하게 빈
  값으로 시작. `onOk` 는 `editor.insertElement(element)` → 현재 선택을 대체.
- 시나리오: 사용자가 "click here" 를 선택 → Mendix Link 다이얼로그에서 마이크로플로우 선택 → OK →
  "click here" 가 삭제되고 필드값(빈 값일 수 있음)으로 대체됨. CK5 `MendixLinkCommand` 는 기존 range 에 attribute 를
  적용해 텍스트를 보존했음.
- 권고: `onShow` 에서 선택이 collapsed 가 아니면 선택 텍스트를 "Link text" 기본값으로 채우고, `onOk` 에서 선택이
  있으면 `insertElement` 대신 선택을 링크로 감싸기 (`editor.getSelection().getRanges()` 순회 + `createBookmarks`).

---

## 🟡 Medium

### C4 — 스크립트 로드 실패 시 rejected promise 가 영구 캐시됨

- 파일: `pluggable/packages/rich-text/src/ckeditor4/loadCKEditor.ts` (`script.onerror`)
- 내용: `script.onerror → reject` 하지만 `pending` (및 `loadedFrom`) 를 리셋하지 않음.
- 시나리오: 일시적 CDN/네트워크 실패나 잠깐 잘못된 커스텀 `editorScriptUrl` 하나로 페이지 수명 내내 모든 Editor
  마운트가 영구 실패 — 이후 `loadCKEditor()` 호출은 `window.CKEDITOR` 가 여전히 undefined 라 캐시된 rejected
  promise 를 반환.
- 권고: `onerror`/`catch` 경로에서 `pending = null; loadedFrom = null` 설정.

### C5 — 외부 값 동기화 effect 가 정규화 차이로 매 렌더 `setData` 호출

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:124`
- 내용: `props.value !== instance.getData()` 를 문자열로 그대로 비교. CKEditor 는 HTML 을 정규화함(속성 순서,
  self-closing, 공백).
- 시나리오: content attribute 가 다른 소스에서 "의미상 동일하지만 텍스트상 다른" HTML 로 갱신되면 `props.value !==
  instance.getData()` 가 계속 true → 매 재렌더마다 `instance.setData()` → 사용자가 타이핑 중인데 캐럿/선택/스크롤이
  리셋됨.
- 권고: 마지막으로 커밋/수신한 정규화 값과 비교하거나, `setData` 후 `getData()` 결과를 기준값으로 저장.

### C7 — devDependency `ckeditor4@4.22.0` 는 `standard` 빌드인데 문서는 `standard-all` 자체 호스팅 안내

- 파일: `pluggable/packages/rich-text/package.json`, `pluggable/packages/rich-text/src/RichText.xml`,
  `pluggable/MIGRATION.md`
- 내용: `RichText.xml` 의 `editorScriptUrl` 설명: 'self-hosted CKEditor 4.22.0 "standard-all" ckeditor.js ... Host
  it inside your app (e.g. theme/web/ckeditor/ckeditor.js)'. 문서대로 `node_modules/ckeditor4/` 를 복사하면 `standard`
  프리셋을 얻고, "full" 툴바/auto-grow 에 필요한 플러그인이 빠짐 (C3).
- 권고: 문서를 `standard` 로 정정하거나, devDep 을 `standard-all` 을 제공하는 소스로 교체하고 기본 CDN URL 도 맞춤.

### C10 — 에디터 생성 effect deps 에서 `minHeight/maxHeight/editorBodyClass/links` 누락

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:118` (`// eslint-disable-next-line react-hooks/exhaustive-deps`)
- 내용: 이 props 들이 마운트 중 변경되면 실행 중인 에디터가 갱신되지 않음(재빌드/재설정 없음) — `preset`/`enterMode`/
  `spellChecker`/`scriptUrl` 때문에 effect 가 우연히 재실행될 때까지.
- 시나리오: Mendix 디자인타임 설정에서는 영향 낮지만, exhaustive-deps 억제가 실제 staleness 버그를 가림 (예:
  microflow-link 목록 변경이 무시됨).
- 권고: `links` 는 최소한 deps 에 포함하고 값 비교로 재설정, 또는 `editor.config` 를 직접 갱신.

---

## 🟢 Low

### C8 — `CKEDITOR.replace()` 가 null 을 반환할 수 있는데 즉시 사용

- 파일: `pluggable/packages/rich-text/src/components/Editor.tsx:78`
- 내용: 호스트 요소에 이미 에디터가 부착돼 있거나(불완전한 StrictMode 정리 / 빠른 deps 변경) `CKEDITOR.env.isCompatible`
  가 false 면 `replace()` 가 null 반환. 다음 줄 `instance.on("instanceReady", ...)` 에서 TypeError → outer `.catch`
  만 잡아서 "Failed to load" 류의 오해 소지 있는 에러 로깅, 에디터 없음.
- 권고: `replace()` 결과 null 체크 후 명확한 에러 처리.

### C9 — `maxCount` 가 0으로 하드코딩되어 관련 코드가 전부 죽음

- 파일: `pluggable/packages/rich-text/src/RichText.tsx:52`
- 내용: `RichText` 가 `maxCount={0}` 을 무조건 전달. `Editor` 는 `props.maxCount > 0 && <div className="rt-count" />`
  로만 사용 → 도달 불가. `EditorProps` 의 `maxCount`, `rt-count` 마크업, `RichText.css` 의 `.rt-count` 규칙은 제거된
  글자 수 기능의 잔재.
- 권고: 죽은 코드 삭제 (또는 글자 수 기능을 CK4 wordcount 플러그인으로 실제 구현).

---

## `react-ver` 리뷰에서 상속되는 이슈 (shared / viewer 미변경)

이 브랜치는 `packages/shared` 와 `packages/rich-text-viewer` 를 `react-ver` 와 동일하게 사용하므로 아래도 유효:

- **[상속-1] 뷰어 XSS** — `shared/src/RichTextView.tsx` 가 저장 HTML 을 sanitize 없이 `dangerouslySetInnerHTML` 로
  렌더. CK4 브랜치는 C1(ACF) 때문에 편집 경로에서는 일부 걸러지지만, 붙여넣기/임포트/마이크로플로우로 세팅된 콘텐츠는
  여전히 뷰어에서 그대로 실행됨. **배포 전 필수.**
- **[상속-2] 이미지 URL 캐시버스터 처닝** — `shared/src/imageUrls.ts` 가 매 호출 `Date.now()` 사용 + 매 렌더 실행
  → 렌더마다 모든 이미지 재다운로드.
- **[상속-3] `keyTimer` 미정리** — `rich-text/src/RichText.tsx` 에 cleanup useEffect 없음 → 언마운트 후
  `onKeyPress.execute()` 발동 가능. (이 브랜치도 동일 코드)
- **[상속-4] 글자 수 UI** — C9 와 동일 맥락 (이 브랜치에서는 `maxCount=0` 하드코딩으로 UI 자체가 죽음).
